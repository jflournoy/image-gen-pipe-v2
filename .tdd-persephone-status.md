# TDD: Persephone Model with Local Flux .1 Dev Encoders

## Current Status: 🟢 GREEN Phase - Automated Integration Test Ready

### ✅ RED Phase Complete (Updated)

Files created and updated for RED phase:

1. **test/flux-persephone-encoders.test.js** - Comprehensive test suite with 3 test groups:
   - **Group 1 - File Existence** (3 tests, ✅ all pass)
     - ✓ T5-XXL FP8 encoder file exists
     - ✓ CLIP-L encoder file exists
     - ✓ Persephone checkpoint file exists
   - **Group 2 - Environment Configuration** (1 test, ✅ passes)
     - ✓ Environment variables can be configured with absolute paths
   - **Group 3 - Automated Integration Test** (spawns Flux service and tests generation)
     - Automatically detects missing Python dependencies (uvicorn, torch, etc.)
     - Provides helpful instructions to install service dependencies
     - Ready to run once `cd services && pip install -r requirements.txt` is executed

2. **docs/PERSEPHONE_LOCAL_ENCODERS.md** - Complete reference guide with:
   - Problem statement and solution overview
   - Environment variable configuration
   - Manual testing procedures
   - Troubleshooting guide

### 🟢 GREEN Phase: Automated Integration Testing

The test suite now includes an automated integration test that spawns the Flux service and validates generation.

#### Step 1: Install Service Dependencies

First time only - install the Flux service's Python dependencies:

```bash
cd services && pip install -r requirements.txt
```

This installs: uvicorn, torch, diffusers, safetensors, and other required packages.

#### Step 2: Run Automated Integration Test

Run the test suite which will:
1. ✓ Verify encoder files exist locally
2. ✓ Verify environment variables can be configured
3. ✓ Spawn Flux service with local encoders
4. ✓ Wait for service to become healthy
5. ✓ Send generation request (1 step, 512x512)
6. ✓ Verify response contains image data
7. ✓ Check for shape mismatch errors in logs
8. ✓ Clean up service gracefully

```bash
npm test -- test/flux-persephone-encoders.test.js
```

**Expected Output:**
```
✓ T5-XXL FP8 encoder file exists
✓ CLIP-L encoder file exists
✓ Persephone checkpoint file exists
✓ Can configure environment for local encoders
✓ Start Flux service with local encoders
✓ Service health check passes
✓ Generates image without shape mismatch errors
✓ Cleanup: Stop Flux service

7 passed
```

**Success Criteria:**
- ✅ All 7 tests pass
- ✅ No "mat1 and mat2 shapes cannot be multiplied" error
- ✅ Response contains image data
- ✅ Service cleans up without hanging

**Known Warnings (Safe to Ignore):**
- ⚠️ "LoRA ... not in list of present adapters" - expected when LoRA disabled
- ⚠️ "CLIP can only handle sequences up to 77 tokens" - expected warning
- ⚠️ Model loading takes 30-60 seconds on first run

### 🔄 REFACTOR Phase

Once Step 4 succeeds, we'll:

1. **Integrate into codebase:**
   - Add environment variable documentation to services/README.md
   - Add example configuration to .env.example
   - Document the pattern for other weight-only Flux models

2. **Update UI Settings:**
   - Add preset for "Persephone + Local Encoders" configuration
   - Show loaded encoder information in service status

3. **Create deployment guide:**
   - Document file organization
   - Provide download links and setup instructions

## Status Summary

| Phase | Task | Status |
|-------|------|--------|
| RED | Create test suite with file existence checks | ✅ Complete |
| RED | Create automated integration test | ✅ Complete |
| RED | Add graceful dependency detection | ✅ Complete |
| RED | Document test requirements | ✅ Complete |
| GREEN | Install service dependencies | ⏳ Pending |
| GREEN | Run automated integration test | ⏳ Pending |
| GREEN | Verify no shape mismatch errors | ⏳ Pending |
| REFACTOR | Integrate into codebase | ⏸️ Waiting for GREEN ✅ |
| REFACTOR | Update documentation | ⏸️ Waiting for GREEN ✅ |

## How to Report Success

Once the automated integration test passes with all 7 tests green, update this file with:

```markdown
GREEN Phase: ✅ COMPLETE
- File existence checks: ✅ PASS
- Environment configuration: ✅ PASS
- Service spawning: ✅ PASS
- Health check: ✅ PASS
- Image generation: ✅ PASS
- No shape mismatch errors: ✅ VERIFIED
- Service cleanup: ✅ PASS
- Ready for REFACTOR phase
```

Then we'll integrate this validated pattern into the main codebase (update docs, add UI presets, create deployment guide).

## Files Modified/Created

- ✅ test/flux-persephone-encoders.test.js (created - comprehensive test suite)
  - File existence checks (3 tests, all pass)
  - Environment configuration (1 test, passes)
  - Automated integration test with service spawning (3 tests, pending)
  - Graceful dependency detection and helpful error messages
- ✅ docs/PERSEPHONE_LOCAL_ENCODERS.md (created - reference guide)
- ✅ public/demo.html (updated - UI improvements for localStorage transparency)
- ✅ public/demo.js (updated - Clear buttons for model path and LoRA)
- ✅ services/flux_service.py (updated - encoder path support via env vars)
- ✅ .env (updated - configuration for persephone + encoders)
- ✅ .tdd-persephone-status.md (tracking document - this file)

## Next Steps After GREEN

Once generation works:

```bash
/tdd implement  # Move to GREEN -> REFACTOR integration
```
